# CHRONOS Production Requirements - MINIMAL
# Install only what's necessary (the large packages like torch are optional)

# ============ Core ============
pyyaml>=6.0
python-dotenv>=1.0.0
pydantic>=2.0.0
python-dateutil>=2.8.2

# ============ Data Processing ============
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# ============ Database & Messaging ============
elasticsearch>=8.0.0
neo4j>=5.0.0
kafka-python>=2.0.2
redis>=4.5.0

# ============ Network & Security ============
python-nmap>=0.7.1
dnspython>=2.4.0
cryptography>=39.0.0
bcrypt>=4.0.0

# ============ Async & API ============
aiohttp>=3.8.0
websockets>=11.0.0
requests>=2.31.0
fastapi>=0.100.0
uvicorn>=0.23.0

# ============ Logging & Monitoring ============
structlog>=23.1.0
prometheus-client>=0.17.0

# ============ Machine Learning (OPTIONAL - LARGE) ============
# torch>=2.0.0              # Uncomment only if needed (500MB)
scikit-learn>=1.3.0         # Alternative to torch for some models

# ============ Visualization ============
plotly>=5.15.0             # Interactive dashboards (optional)

# ============ Testing (Optional) ============
pytest>=7.0.0
pytest-asyncio>=0.21.0
